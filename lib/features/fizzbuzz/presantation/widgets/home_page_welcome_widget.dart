import 'package:flutter/material.dart';

class HomePageWelcomeWidget extends StatelessWidget {
  const HomePageWelcomeWidget({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Welcome to FizzBuzz'));
  }
}
